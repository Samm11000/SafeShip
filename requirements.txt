flask==3.0.3
boto3==1.34.69
scikit-learn==1.4.2
imbalanced-learn==0.12.3
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0
pydantic==2.7.1
joblib==1.4.2
requests==2.31.0
gunicorn==22.0.0

# --- Observability & hardening (Phase 1) -----------------------------------
# sentry-sdk and flask-limiter are optional at runtime: main.py degrades to a
# warning if they are absent, so a slim deploy still boots.
flask-limiter==3.8.0
sentry-sdk[flask]==2.14.0
