# Development & test dependencies.
# The suite mocks AWS entirely with moto — no credentials, no cloud calls.
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest tests/ -q
-r requirements.txt
pytest==8.4.2
moto[s3,dynamodb]==5.1.22
